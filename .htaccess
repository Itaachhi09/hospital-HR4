<IfModule mod_rewrite.c>
RewriteEngine On

# Route /api/* to PHP router while serving existing files directly if present
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

RewriteRule ^api/(.*)$ php/api/rest.php/$1 [QSA,L]
</IfModule>

